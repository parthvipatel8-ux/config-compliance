# Compliance Intelligence API

AI-powered CIS benchmark remediation API — semantic search + RAG over parsed compliance controls, served via FastAPI.

**Status:** work in progress (deploy weekend). Full docs, setup guide, and eval results coming.

*Note: CIS benchmark PDFs and derived data are not included in this repo due to CIS licensing — the pipeline expects them under `data/` (see `parse_benchmark.py`).*